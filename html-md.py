# html-md
# Convert HTML to markdown 

import argparse

# Initialization of argument parser used for CLI
parser = argparse.ArgumentParser(description='Convert HTML files to MD files')

parser.addArgument('-i', '--infile', )

